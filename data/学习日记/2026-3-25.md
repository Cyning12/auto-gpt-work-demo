# Day9 学习日志：词向量（Word2Vec）与句向量 API——语料管线、落盘与检索

**📅 日期**：2026-03-25  
**📌 定位**：在「三国演义」语料上跑通 **本地 Word2Vec** 与 **DashScope 文本向量 API** 两条链路，理解 **词级 vs 段级** 表示，并能把 **问句** 接到各自的检索方式上。

---

## 一、语料与目录约定（和 PathLineSentences 的关系）

- **原文**：`data/课程练习/source/{语料名}/` 下各文件。  
- **分词结果**：写在 **`segmented/{语料名}/`**，与 `source` **同级**（不要放在 `source/.../segmented`），避免 `PathLineSentences` 把未分词正文和分词行混在一起读。  
- **Word2Vec 模型**：`vectorModels/{语料名}/{语料名}_word2vec.model`。  
- **Gensim `model.save`**：参数必须是 **字符串路径** 或带 `write` 的文件对象；传 `pathlib.Path` 会在内部 `.endswith` 等处报错。

> **[插图占位]**：`课程练习/source` 与 `课程练习/segmented`、`vectorModels` 并列目录树

---

## 二、Word2Vec：词表、`most_similar` 与「句子查询」

- **`wv.most_similar("曹操")`**：只接受 **词表里存在的 token**；不能把整句当作一个词 key（会 `KeyError`）。  
- **想用「句子 / 问句」在词向量空间里找近邻词**：`jieba` 分词 → 过滤 OOV → 对剩余词向量 **取平均** → **`wv.similar_by_vector`**。练习里封装为 **`similar_words_for_sentence`**。  
- **局限**：得到的是与句向量方向接近的 **词**，不是「另一段原文句子」排序；**句对段** 检索更适合下面的 API 向量库或 Doc2Vec。

**类比任务**：`demo_query_changban_adou_similarity` 与 API 侧同名函数使用同一问句 **「谁在长坂坡救了阿斗？」**，便于两条路线对照（默认不在 `__main__` 里自动调用）。

---

## 三、DashScope Text Embedding：批量建库、本地复用与 TopK

- **建库**：枚举语料目录下文件 → 长文按字符上限 **切块** → 分批 `TextEmbedding.call` → `pickle` 写入 **`{语料名}_API.model`**（与 Word2Vec 同放在 `vectorModels/{语料名}/`）。  
- **`embeddings` 结构**：`output["embeddings"]` 是 **列表**，每项含 **`embedding`（浮点向量）** 与 **`text_index`**；取单条输入的向量用 `[0]["embedding"]`。  
- **`skip_if_exists`**：本地已有 `*_API.model` 时 **跳过重新编码**，省 API；但对 **新问句** 做相似度仍要对 query **再调一次** Embedding（除非自行传入已算好的 `query_vector`）。  
- **检索**：对 query 与库中各块向量算 **余弦相似度**，取 TopK；条目里可带 **`snippet`** 便于肉眼扫结果。

**额度**：若返回 **403 / `AllocationQuota.FreeTierOnly`**，说明免费额度用尽，需在控制台关闭「仅使用免费额度」或开通按量付费；脚本里可对失败信息做 **中文说明**，并用 **本地第一条语料向量** 做 **离线 TopK 演示**（不调 API，仅演示库内向量排序）。

---

## 四、两条路线怎么选（心里要有数）

| 维度 | Word2Vec（本地） | 文本 Embedding API |
|------|------------------|---------------------|
| 粒度 | **词** | **整段 / 切块文本** |
| 典型查询 | 单词近邻、加减类比 | 问句 → 与语料块相似度 |
| 依赖 | 分词语料、训练时间 | 网络、Key、配额 |
| 离线 | 训练后可完全本地 | 库可离线读；**新 query** 一般仍要联网 |

---

## 五、遗留与可加深

- Word2Vec **句向量** 仅用平均词向量是基线；可了解 **加权（TF-IDF）**、**Doc2Vec**、或多语言 **句向量模型** 与 RAG 里 chunk 策略。  
- API 侧切块长度与 **`text_type`（query / document）** 对检索分布的影响，可在有额度时做小规模对比实验。

---

## 六、相关文件（便于回跳）

- `data/课程练习/Embeddings和向量数据库/embedding_Word2Vec.py`  
- `data/课程练习/Embeddings和向量数据库/embedding_API.py`  
- 语料与产物：`source/`、`segmented/`、`vectorModels/`（均在 `data/课程练习/` 下）
